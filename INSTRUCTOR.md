# 講師用メモ (INSTRUCTOR.md)

このファイルは受講者には配布しません。

## このリポジトリの位置づけ

- TS研修3日目の演習素材
- Day 1〜2で「型システムの理解」「narrowing」「ジェネリクス」「ユーティリティ型」を終えた前提
- Day 3はこの課題集を **個人戦 + 質問OK** で進める

## 想定スケジュール (Day 3)

| 時間 | 内容 |
|------|------|
| 09:30 - 10:00 | オリエンテーション、進め方の説明、セットアップ確認 |
| 10:00 - 12:00 | 課題 01〜03 |
| 12:00 - 13:00 | 昼休み |
| 13:00 - 15:00 | 課題 04〜06 |
| 15:00 - 16:30 | 課題 07 (最大の山場) |
| 16:30 - 17:00 | 振り返り + 総括 + 後続研修 (React/Next.js) への橋渡し |

## チェックポイント設計

以下の3箇所で講師が受講者を捕まえて進捗確認 + 軽いコードレビュー:

- 課題 03 クリア時点 (基礎編の終わり)
- 課題 06 クリア時点 (応用編の終わり)
- 課題 07 クリア時点 (総合演習の終わり)

これが混在対策にもなる。遅れている人には重点的にフォロー。

## 判定の仕組み

- `npm test` で vitest が JSON を出力 (`public/test-results.json`)
- `npm run dev` の Vite ページが JSON を読んで進捗を表示
- 型レベルのテスト (`expectTypeOf`) + `@ts-expect-error` により、`any` で逃げた提出を検知できる

## よくあるつまずきポイント

- **課題01**: `any` を `number` に直したのに、`expectTypeOf` で落ちる → パラメータ型と戻り値型の両方を見るよう促す
- **課題02**: Discriminated Union のタグを忘れて `status` をまたいだ型にしてしまう
- **課題03**: ジェネリクスの `extends` 制約を付けずに `keyof` が引けないエラー
- **課題07**: エラー型の 2パターン (`validation` / `server`) を1つの型にまとめてしまい、`retryAfter` へのアクセスで narrowing が必要なことに気づかない

## 振り返りパートで話すと効くこと (16:30以降)

- 今日一番詰まったのはどこか、全員に一言ずつ
- 講師側から「このつまずきは React 研修でもこう出てくる」という予告
  - Discriminated Union → reducer の action 型 / props のバリアント
  - Narrowing → `useState<T | null>` のあとのチェック
  - Generics → `useState<T>`, 汎用コンポーネント
  - ユーティリティ型 → props の派生、API型からフォーム型を作る
- 「今日できなかった課題も、配属後に見直すと別物に見える」と伝える

## 課題の全体解答

`solutions/` ブランチに用意 (受講者用 main ブランチには含めない)。
